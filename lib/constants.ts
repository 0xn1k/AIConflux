export const DEFAULT_FREE_MODELS = ["ChatGPT", "DeepSeek", "Gemini"];
export const PREMIUM_MODELS = ["Claude", "Perplexity", "Grok"];
export const ALL_MODELS = [...DEFAULT_FREE_MODELS, ...PREMIUM_MODELS];

// Initial tokens given to new users
export const INITIAL_TOKENS = 10;
